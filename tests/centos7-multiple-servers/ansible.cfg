[defaults]
hostfile = hosts
remote_user = vagrant
# private_key_file = .vagrant/machines/miarec/virtualbox/private_key
# private_key_file = ~/.vagrant.d/insecure_private_key
host_key_checking = False    # Do this for vagrant only. Dangerous for production
roles_path = ../../roles
